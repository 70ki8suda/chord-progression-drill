{
  "scripts": {
    "build": "node tools/build-pages.mjs",
    "deploy": "npm run build && wrangler pages deploy dist --project-name chord-progression-drill",
    "test": "node tools/smoke-test.mjs"
  }
}
